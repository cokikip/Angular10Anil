import { CommentTextFilterPipe } from './comment-text-filter.pipe';

describe('CommentTextFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentTextFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
